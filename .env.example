# GitHub OAuth App credentials for the Sveltia CMS editor login.
# Create an OAuth App at https://github.com/settings/developers with
# Authorization callback URL = https://tuucan-site.vercel.app/api/callback
# Then set both values in the Vercel project (Production env).
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
